name: wexcom_cloud_server
description: Wexcom cloud sync server — raw SQLite file upload/download.
version: 1.0.0

environment:
  sdk: ^3.4.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  crypto: ^3.0.6
  args: ^2.7.0
  sqlite3: ^2.4.6
  path: ^1.9.0
