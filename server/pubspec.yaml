name: wexcom_sync_server
description: Optional sync server for wexcom mobile app.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  args: ^2.7.0
  crypto: ^3.0.6
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4
  sqlite3: ^2.9.3
